import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "odprz-page",
  outDir: './dist/static',
  staticPort: 1400,
  appPort: 1401,
  routes: {
  },
};
